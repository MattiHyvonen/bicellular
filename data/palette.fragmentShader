#version 330 core

in vec2 UV;
uniform sampler2D tx;

out vec3 outColor;

uniform mat4 colorTransform;

void main() {
    float txColor = texture(tx, UV).r;
    outColor = vec3(txColor, -txColor, -txColor);
}
