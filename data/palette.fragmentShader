#version 330 core

in vec2 UV;
uniform sampler2D tx;

out vec3 outColor;

uniform mat4 colorTransform;

void main() {
    float txColor = texture(tx, UV).r;
    float outValue = (txColor + 1)/2;
    outColor = vec3(outValue, outValue, outValue);
}
